import React from 'react';
// import Button from '../utils/Button';

const Main: React.FC = () => {
  return (
    <main>
      Main
      {/* <Button label='Add Transaction' onClick={() => console.log('hejja')}/> */}
    </main>
  );
};

export default Main;
